version: '3'

services:
  ensure-indices:
    build:
      context: ./
      dockerfile: Dockerfile
    network_mode: "host"
    volumes:
      - ../../config:/scripts/ensure-indices
      - ${HOST_LOG_DIR}:/scripts/log
